# Hero 3D model

Current hero model:

```
public/models/hero-tapbar.glb
```

Path constant: `HERO_MODEL_PATH` in `components/sections/HeroCanvas.tsx`.

Replace the file to swap models (`.glb` recommended).
